name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /home/ubuntu/sales-portal-server

            echo "Pull latest code"
            git pull origin main

            echo "📦 Install dependencies"
            npm ci

            echo "🏗️ Build project"
            npm run build

            echo "🔁 Restart PM2"
            pm2 reload sales-portal || pm2 start dist/main.js --name sales-portal

            echo "💾 Save PM2"
            pm2 save
